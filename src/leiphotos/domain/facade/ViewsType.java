package leiphotos.domain.facade;

/**
 *
 * Represents the different library views that the application supports. 
 * 
 * @author malopes
 */	
public enum ViewsType {
	ALL_MAIN, //all photos in the main lib
	ALL_TRASH, //all photos in the trash lib
	FAVOURITES, // favourite photos in the main lib
	MOST_RECENT; // most recently viewed photos in the main lib
}
